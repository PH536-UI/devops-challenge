
Completei o 7 Day DevOps Challenge.
Construí um pipeline CI/CD completo do zero — sem gastar nada, direto do WSL Ubuntu usando LocalStack + Terraform + GitHub Actions.

Cada dia foi um bloco real: EC2 → Git → S3 → Build → Deploy → CloudFormation → CodePipeline.

O mais valioso? Debugar na raça. Heredoc quebrando no terminal, lock file conflitando, S3 recusando upload por header não suportado — cada erro foi uma aula que nenhum tutorial entrega.

No final: curl -I localhost:8080 → HTTP/1.1 302. App no ar. Pipeline funcionando.
#DevOps#AWS#Terraform #CICD#GitHubActions#CloudComputing #LocalStack#CloudFormation#Java#OpenToWork
